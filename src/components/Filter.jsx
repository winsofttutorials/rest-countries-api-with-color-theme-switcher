import React from "react";

const Filter = () => {
  return (
    <section className="f-section">
      <form onSelect={() => {}}>
        <input type="select" />
      </form>
    </section>
  );
};

export default Filter;
